-- Central configuration for this local-only client build.
-- Change these values when moving the client to another server; recompilation
-- is not required.
return {
    loginUrl = "http://127.0.0.1/login.php",
    webUrl = "http://127.0.0.1",
    loginPort = 80,
    protocol = 1530
}
